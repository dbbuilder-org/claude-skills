{
  "name": "paste-as-image",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Render a large clipboard/file blob to dense PNG pages via pxpipe so Claude Code reads it as an image instead of text tokens.",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "gpt-tokenizer": "^3.4.0",
    "pxpipe-proxy": "^0.8.0"
  }
}
